# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'lowestprice' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for lowestprice
  pod 'FirebaseCore'
  pod 'Firebase/Database'
  
  # FIXME: bug in iOS
  # pod 'FirebaseFirestore'
  # pod 'BoringSSL', '~> 9.1'
  # pod 'gRPC-Core', :git => 'https://github.com/mrmage/grpc', :branch => 'fix-cocoapods-swift'
  # pod 'SwiftGRPC', :git => 'https://github.com/mrmage/grpc-swift', :branch => 'podspec'

  target 'lowestpriceTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
